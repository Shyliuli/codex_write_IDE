1.（✅ 已修复）获取 rustic.hpp 后 LSP 会自动重启并重新加载 include，`#include "rustic.hpp"` 红波浪与导航问题已解决。
2.（✅ 已修复）调试入口和单步/继续/停止等功能已在工具栏与调试菜单中单独列出，体验对齐 VSCode。
3.（✅ 已修复）断点命中后会自动跳转并高亮当前执行行。
4.（✅ 已修复）行号区宽度已加大，断点图标不再遮挡行号。
5.（✅ 已修复）项目树现在默认显示所有文件；Groups 视图中也会列出 Makefile 与其他非源文件。
6.（✅ 已修复）补全列表会按当前前缀过滤，只展示匹配项。
7.（✅ 已修复）补全触发改为默认输入即触发（不再依赖 `::` 等），且注释/字符串内不触发。
8.（✅ 已修复）调试已补全到 VSCode 基本形态：线程/栈/局部变量/监视表达式、条件断点、命中次数、日志断点等。
